apiVersion: v1
kind: Service
metadata:
  name: site-orcamentos
  namespace: site-orcamentos
spec:
  selector:
    app: site-orcamentos
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
