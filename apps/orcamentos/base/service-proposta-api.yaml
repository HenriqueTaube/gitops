apiVersion: v1
kind: Service
metadata:
  name: proposta-api
  namespace: site-orcamentos
spec:
  selector:
    app: proposta-api
  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: 8000
