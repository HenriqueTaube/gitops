apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: wireguard-longhorn
  namespace: wireguard
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 1Gi
