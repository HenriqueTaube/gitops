apiVersion: v1
kind: Service
metadata:
  name: mempool-backend
  namespace: mempool
spec:
  selector:
    app: mempool-backend
  ports:
    - name: http
      protocol: TCP
      port: 8999
      targetPort: 8999
