apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: loki-longhorn
  namespace: loki
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: longhorn
  resources:
    requests:
      storage: 20Gi
