apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: longhorn-test-rasp
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn-2
  resources:
    requests:
      storage: 2Gi
