apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: longhorn-test-prox
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn-2
  resources:
    requests:
      storage: 2Gi
